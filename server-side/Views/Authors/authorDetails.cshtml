@model service.Models.viewModel
@{
    ViewBag.Title = "authorDetails";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


    

        <div class="panel-group">
            <div class="panel panel-default">
                <div class="panel-heading">@Model.author.FirstName @Model.author.LastName</div>
                <div class="panel-body">

                    <small>Birth year: @Model.author.BirthYear</small>
                    <br />
                    <small>unique ID: @Model.author.Aid</small>
                    <br />
                    <small>Books:</small>
                    @foreach (var books in Model.bookList)
                    {
                        <small>@books.Title</small>
                    }
                </div>
            </div>
        </div>


    




